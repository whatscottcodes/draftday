[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "draft-night-backend"
version = "1.0.0"
description = "Draft Night V1 backend"
requires-python = ">=3.12"

[tool.setuptools]
packages = ["app", "app.api", "app.draft", "app.services"]